server {
    listen 80;
    server_name currency-exchanger.com;

    location /media/ {
       root root /home/yuliya/currency/static_content/media;
    }

    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }
}

#gunicorn
[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/yuliya/currency/src
ExecStart=/home/yuliya/currency/env/bin/gunicorn -w 4 --bind 0.0.0.0:8000 settings.wsgi

[Install]
WantedBy=multi-user.target

#celery
[Unit]
Description=celery daemon
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/yuliya/currency/src
ExecStart=/home/yuliya/currency/env/bin/celery -A settings worker -E --loglevel=info

[Install]
WantedBy=multi-user.target

#celerybeat
[Unit]
Description=celery beat daemon
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/yuliya/currency/src
ExecStart=/home/yuliya/currency/env/bin/celery -A settings beat --loglevel=info

[Install]
WantedBy=multi-user.target

# postgres
# db-name: currency_exchanger
# user: ubuntu
# password: skadlhfp231u*&(*

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'currency',
        'USER': 'currency',
        'password': '123456',
        'HOST': 'localhost',
        'PORT': '',
    }
}